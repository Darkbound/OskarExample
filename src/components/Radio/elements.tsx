import { Radio, RadioGroup } from "@material-ui/core";
import styled from "styled-components";

export const StyledRadio = styled((props) => <Radio {...props} />)``;

export const StyledRadioGroup = styled((props) => <RadioGroup {...props} />)``;
